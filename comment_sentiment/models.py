from django.db import models
from .model import predict_sentiment

# class CommentManager(models.Manager):
#     def make_sentiment_analysis(self):
#
#
# class Comment(models.Model):
#     text = models.TextField(name='text', verbose_name='text')
#     rating = models.IntegerField(name='rating', verbose_name='rating', default=10)
#     is_pos = models.BooleanField(name='is_pos', verbose_name='is_pos', default=True)


